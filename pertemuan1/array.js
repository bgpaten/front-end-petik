// membuat array : [] & array()
const fruits = ["Apel", "Anggur", "Jeruk"];

// mengakses nilai
console.log(fruits[1]);

// mengakses semua niali
// Looping
for (let i = 0; i < fruits.length; i++) console.log(fruits[i]);

//Looping for-of
for (const fruit of fruits) console.log(fruit);
